{
  "name": "@wonseoro/document-service",
  "version": "0.0.1",
  "private": true,
  "description": "서류 서비스 — Presigned Upload, Hash/MIME 재검증, AV 검사 상태 관리",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@wonseoro/contracts": "*"
  }
}
