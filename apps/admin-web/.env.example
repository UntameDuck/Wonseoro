# 관리자 콘솔 — 서버 쪽에서만 읽는다. 브라우저에 내려가지 않는다. (NEXT_PUBLIC_ 을 붙이지 않는다)
ADMISSION_API_URL=http://localhost:3001
# admission-api 의 ADMIN_API_TOKEN 과 같은 값. 비우면 admission-api 도 비어 있어야 한다 (개발 전용).
ADMIN_API_TOKEN=
