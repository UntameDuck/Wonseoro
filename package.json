{
  "name": "wonseoro",
  "version": "0.0.1",
  "private": true,
  "description": "원서로 / K-Admission — K-PaaS 기반 분산형 대학입학 원서접수 표준 플랫폼",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:infra": "docker compose -f infra/compose/docker-compose.dev.yml up -d",
    "dev:infra:down": "docker compose -f infra/compose/docker-compose.dev.yml down",
    "dev:university": "npm run dev -w @wonseoro/university-api",
    "dev:central": "npm run dev -w @wonseoro/central-api",
    "dev:web": "npm run dev -w @wonseoro/applicant-web",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build:contracts": "npm run build -w @wonseoro/contracts",
    "db:migrate": "docker exec -i wonseoro-dev-postgres-univ-a-1 psql -U wonseoro -d univ_a -v ON_ERROR_STOP=1 < infra/db/migrations/0001_init.sql",
    "db:verify": "docker exec -i wonseoro-dev-postgres-univ-a-1 psql -U wonseoro -d univ_a -v ON_ERROR_STOP=1 < infra/db/verify-constraints.sql",
    "db:seed": "docker exec -i wonseoro-dev-postgres-univ-a-1 psql -U wonseoro -d univ_a -v ON_ERROR_STOP=1 < infra/db/seed-dev.sql",
    "db:migrate:central": "docker exec -i wonseoro-dev-postgres-central-1 psql -U wonseoro -d central -v ON_ERROR_STOP=1 < infra/db/central/0001_init.sql && docker exec -i wonseoro-dev-postgres-central-1 psql -U wonseoro -d central -v ON_ERROR_STOP=1 < infra/db/central/0002_vault.sql",
    "dev:relay": "npm run dev -w @wonseoro/event-relay",
    "dev:scanner": "npm run dev -w @wonseoro/document-service"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
